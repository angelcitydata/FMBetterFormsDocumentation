export const metadata = {
  title: 'Badge Component',
  description:
    'A versatile badge component for FM BetterForms that displays labels, status indicators, and notifications with customizable variants, icons, and dismissible functionality.',
}

# Badge Component

The Badge component provides a compact way to display labels, status indicators, and notifications in your FM BetterForms application. It features customizable variants, optional icons, pill or rounded shapes, and dismissible functionality for creating clear visual indicators and user feedback. {{ className: 'lead' }}

    This component supports multiple color variants, left and right icons,
    dismissible functionality with named actions, and responsive design
    for displaying status, categories, and important information.

---

## Component Properties

### Content Properties

<Properties>
  <Property name="label" type="string">
    The text content displayed in the badge. This should be concise and
    descriptive of the status, category, or information being conveyed.
    Example: `"Active"`, `"Pending"`, `"New"`, `"Beta"`
  </Property>
  <Property name="variant" type="string">
    The visual style variant of the badge. Controls the background color,
    text color, and border styling. Available variants: `"default"`,
    `"primary"`, `"secondary"`, `"destructive"`, `"outline"`, `"success"`,
    `"warning"`, `"info"`. Default is `"default"`.
  </Property>
  <Property name="icon" type="string">
    Font Awesome icon class for the left side of the badge. Example:
    `"fa-check"`, `"fa-star"`, `"fa-bell"`. Default is empty string.
  </Property>
  <Property name="iconRight" type="string">
    Font Awesome icon class for the right side of the badge. Example:
    `"fa-arrow-right"`, `"fa-external-link"`. Default is empty string.
  </Property>
</Properties>

### Appearance Properties

<Properties>
  <Property name="pill" type="boolean">
    Whether the badge should have fully rounded corners (pill shape) or
    slightly rounded corners. When `true`, the badge uses `rounded-full`.
    When `false`, it uses `rounded-md`. Default is `false`.
  </Property>
  <Property name="dismissible" type="boolean">
    Whether the badge should display a dismiss button (X) on the right side.
    When `true`, a small X button appears that can trigger a named action.
    Default is `false`.
  </Property>
</Properties>

### Behavior Properties

<Properties>
  <Property name="namedAction" type="string">
    The named action to trigger when the dismiss button is clicked. Only
    relevant when `dismissible` is `true`. Example: `"removeBadge"`,
    `"clearNotification"`. Default is empty string.
  </Property>
  <Property name="actionOptions" type="object">
    Additional options or parameters to pass to the named action when
    the dismiss button is clicked. Default is empty object `{}`.
  </Property>
  <Property name="windowIndex" type="number">
    Index of the window in the BetterForms window stack. Used for multi-window
    applications to access the correct model state. Default is 0.
  </Property>
</Properties>

---

## Badge Variants

The Badge component supports multiple visual variants that control the appearance and color scheme. Each variant provides a distinct look for different use cases and contexts.

### Available Variants

- **`default`** - Dark background with light text (default)
- **`primary`** - Primary brand color background
- **`secondary`** - Secondary/muted background color
- **`destructive`** - Red/danger color for errors or warnings
- **`outline`** - Transparent background with border
- **`success`** - Green color for success states
- **`warning`** - Yellow/amber color for warnings
- **`info`** - Blue color for informational content

### Variant Usage Guidelines

- Use **`primary`** for important status indicators
- Use **`success`** for completed states or positive actions
- Use **`warning`** for pending states or attention-required items
- Use **`destructive`** for errors, deletions, or critical issues
- Use **`info`** for informational badges or tips
- Use **`outline`** for subtle indicators that don't need emphasis
- Use **`default`** for general-purpose badges

---

## Usage Examples

The Badge component is configured using JSON objects with properties for text, styling, icons, and behavior. Here are common usage patterns for different scenarios.

### Basic Status Badge

<CodeGroup>
```html title="HTML Component"
<bfcomp name="Badge" 
        label="Active" 
        variant="success">
</bfcomp>
```

```javascript title="Model Definition"
// Define badge in your model
model.statusBadge = {
  label: 'Active',
  variant: 'success'
}
```

```json title="JSON Configuration"
{
  "label": "Active",
  "variant": "success"
}
```

</CodeGroup>

### Badge with Icon

<CodeGroup>
```html title="HTML Component"
<bfcomp name="Badge" 
        label="New" 
        variant="primary"
        icon="fa-star">
</bfcomp>
```

```javascript title="Model Definition"
// Define badge with icon in your model
model.newBadge = {
  label: 'New',
  variant: 'primary',
  icon: 'fa-star'
}
```

```json title="JSON Configuration"
{
  "label": "New",
  "variant": "primary",
  "icon": "fa-star"
}
```

</CodeGroup>

### Dismissible Notification Badge

<CodeGroup>
```html title="HTML Component"
<bfcomp name="Badge" 
        label="Notification" 
        variant="info"
        icon="fa-bell"
        dismissible="true"
        namedAction="clearNotification">
</bfcomp>
```

```javascript title="Model Definition"
// Define dismissible badge in your model
model.notificationBadge = {
  label: 'Notification',
  variant: 'info',
  icon: 'fa-bell',
  dismissible: true,
  namedAction: 'clearNotification'
}
```

```json title="JSON Configuration"
{
  "label": "Notification",
  "variant": "info",
  "icon": "fa-bell",
  "dismissible": true,
  "namedAction": "clearNotification"
}
```

</CodeGroup>

### Pill-Shaped Badge

<CodeGroup>
```html title="HTML Component"
<bfcomp name="Badge" 
        label="Beta" 
        variant="warning"
        pill="true">
</bfcomp>
```

```javascript title="Model Definition"
// Define pill-shaped badge in your model
model.betaBadge = {
  label: 'Beta',
  variant: 'warning',
  pill: true
}
```

```json title="JSON Configuration"
{
  "label": "Beta",
  "variant": "warning",
  "pill": true
}
```

</CodeGroup>

### Badge with Right Icon

<CodeGroup>
```html title="HTML Component"
<bfcomp name="Badge" 
        label="External Link" 
        variant="outline"
        iconRight="fa-external-link">
</bfcomp>
```

```javascript title="Model Definition"
// Define badge with right icon in your model
model.linkBadge = {
  label: 'External Link',
  variant: 'outline',
  iconRight: 'fa-external-link'
}
```

```json title="JSON Configuration"
{
  "label": "External Link",
  "variant": "outline",
  "iconRight": "fa-external-link"
}
```

</CodeGroup>

### Error Status Badge

<CodeGroup>
```html title="HTML Component"
<bfcomp name="Badge" 
        label="Error" 
        variant="destructive"
        icon="fa-exclamation-triangle">
</bfcomp>
```

```javascript title="Model Definition"
// Define error badge in your model
model.errorBadge = {
  label: 'Error',
  variant: 'destructive',
  icon: 'fa-exclamation-triangle'
}
```

```json title="JSON Configuration"
{
  "label": "Error",
  "variant": "destructive",
  "icon": "fa-exclamation-triangle"
}
```

</CodeGroup>

---

## Component Schema

The complete component schema for the Badge component. Copy and paste this code into the Component Schema section of your BetterForms app.

### Complete Schema

<CodeGroup>
```json title="Component Schema"
{
    "actionOptions": {},
    "dismissible": false,
    "html": "<span\n  :class=\"\n    // base\n    'inline-flex items-center gap-1.5 w-fit select-none text-xs font-medium px-2.5 py-1 border transition-colors ' +\n    // shape\n    (schema.pill ? 'rounded-full ' : 'rounded-md ') +\n    // variant\n    (\n      schema.variant==='primary'      ? 'bg-primary text-primary-foreground border-transparent' :\n      schema.variant==='secondary'    ? 'bg-secondary text-secondary-foreground border-transparent' :\n      schema.variant==='destructive'  ? 'bg-destructive text-destructive-foreground border-transparent' :\n      schema.variant==='outline'      ? 'bg-transparent text-foreground border-border' :\n      schema.variant==='success'      ? 'bg-success text-foreground border-transparent' :\n      schema.variant==='warning'      ? 'bg-warning text-foreground border-transparent' :\n      schema.variant==='info'         ? 'bg-info text-foreground border-transparent' :\n                                        'bg-foreground text-background border-transparent'\n    )\n  \"\n>\n  <!-- Left icon -->\n  <i v-if=\"schema.icon\" class=\"fa-regular\" :class=\"schema.icon\"></i>\n\n  <!-- Label -->\n  <span>{{ schema.label || 'Badge' }}</span>\n\n  <!-- Right icon -->\n  <i v-if=\"schema.iconRight\" class=\"fa-regular\" :class=\"schema.iconRight\"></i>\n\n  <!-- Dismiss -->\n  <button\n    v-if=\"schema.dismissible\"\n    type=\"button\"\n    class=\"ml-1 inline-flex h-4 w-4 items-center justify-center rounded focus:outline-none focus:ring-2 focus:ring-ring hover:bg-accent hover:text-accent-foreground\"\n    aria-label=\"Remove badge\"\n    @click=\"schema.namedAction && namedAction(schema.namedAction, schema.actionOptions)\"\n  >\n    <i class=\"fa-regular fa-xmark text-[10px]\"></i>\n  </button>\n</span>",
    "icon": "",
    "iconRight": "",
    "label": "Badge",
    "namedAction": "",
    "pill": false,
    "variant": "default",
    "windowIndex": 0
}
```
</CodeGroup>

### How to Add This Component to Your BetterForms App

1. **Navigate to Components**: In your BetterForms site environment, click the **Components** tab in the left sidebar.

2. **Create New Component**: If the Badge component is not already in your components list, click the **+New** button at the top left.

3. **Open Component Schema**: In the new component, click the **`</> Component Schema`** button to access the schema editor.

4. **Replace Schema Code**: Copy the complete schema from the **Component Schema** tab above and replace all the existing code in the Component Schema section.


5. **Save Component**: Click the **Save** button in the top right to save the component for use in your application.

6. **Refresh Pages**: **Important**: The component will not be available for use in any live pages you are currently viewing until you manually refresh the page using your browser's Refresh button. However, the component will be immediately available in the **Page Builder** section of every page after you save and navigate away from the Components section.


